<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="essay">

<select id="getJobCate" resultType="java.util.HashMap">
select catename from jobcate
</select>
<insert id="setMyEssay" parameterType="java.util.HashMap">
insert into jaso values(#{jasono}, #{title},#{id}, #{job},#{Q1},#{Q2},#{Q3},#{Q4},#{A1},#{A2},#{A3},#{A4},sysdate)
</insert>
<select id="getMyEssay" parameterType="java.lang.String" resultType="java.util.HashMap">
select jasono,writer,title,lastdate from jaso where writer=#{id}
</select>

<select id="getDetailEssay" parameterType="java.lang.String" resultType="java.util.HashMap">
select * from jaso where jasono=#{jasono}
</select>
<update id="updateEssay" parameterType="java.util.HashMap">
update jaso set title=#{title},JOBCATE=#{job},lastdate=sysdate,q1=#{Q1},a1=#{A1},q2=#{Q2},a2=#{A2},q3=#{Q3},a3=#{A3},q4=#{Q4},a4=#{A4} where jasono=#{no}
</update>


</mapper>